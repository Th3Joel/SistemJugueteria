pipeline:
  name: Build SistemJugueteria
  identifier: Build_Th3Joel_SistemJugueteria_1730735409948
  projectIdentifier: default_project
  orgIdentifier: default
  stages:
    - stage:
        name: Build
        identifier: Build
        type: CI
        spec:
          cloneCodebase: true
          execution:
            steps:
              - step:
                  type: Run
                  name: Install dependency of frontend
                  identifier: Echo_Welcome_Message
                  spec:
                    shell: Bash
                    command: cd web && ls && pwd && bun i
              - step:
                  type: Run
                  name: install mod gol
                  identifier: install_mod_gol
                  spec:
                    shell: Sh
                    command: go mod tidy
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Docker
            spec: {}
  properties:
    ci:
      codebase:
        connectorRef: account.Github_OAuth_1730655910162
        repoName: Th3Joel/SistemJugueteria
        build: <+input>
